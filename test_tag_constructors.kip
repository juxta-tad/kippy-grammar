## Bare constructors as first-class values
let none = None
let empty = Empty

## Constructors with arguments
let some_value = Some(42)
let result_ok = Ok(value)
let result_err = Err("message", code)

## Multiple arguments
let variant = Either(Left("x"), Right(1))

## Nested constructors
let nested = Wrapper(Inner(data))
