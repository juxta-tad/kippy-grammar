@deprecated("old")
@optimize(inline: true, level: 2)
pub let cert myFunc : Int -> String = fn x: "result"

sig process : a, b -> String where a : Show

type Result(Ok, Err) = [Ok(Ok), Err(Err)]

ability Reader
  read : File -> Bytes

when x is
  1 => "one"
  _ => "other"
